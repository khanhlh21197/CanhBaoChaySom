package com.example.firewarning.dialog;

import android.content.Context;
import android.widget.DigitalClock;

public class Clock extends DigitalClock {
    public Clock(Context context) {
        super(context);
    }
}
